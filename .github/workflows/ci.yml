name: CI

# Pull requests and non-main branches get the same checks as a deploy, minus
# the publish. Pushes to main are covered by the deploy workflow.
on:
  pull_request:
  push:
    branches-ignore: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Lint
        run: npm run lint

      - name: Imports resolve case-sensitively
        # Windows and macOS resolve imports case-insensitively; Linux does not.
        run: npm run check:imports

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests
        run: npm test

      - name: Schema and RPC tests
        # Runs the migrations and the SQL assertions against PGlite (Postgres
        # compiled to WASM), so the schema is exercised in CI without needing a
        # database service.
        run: npm run test:sql

      # The build is part of the gate: a type error the app config tolerates
      # can still break the production bundle.
      - name: Build
        run: npm run build

      # Fails only on a *known* vulnerability at high severity or above, so a
      # transitive advisory in a dev dependency does not block a deploy.
      - name: Audit dependencies
        run: npm audit --audit-level=high --omit=dev
